import 'package:flutter/material.dart';

import 'landing_label.dart';

class CopyrightFooterView extends StatelessWidget {
  const CopyrightFooterView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const LandingLabel('TODO: Add text here');
  }
}
