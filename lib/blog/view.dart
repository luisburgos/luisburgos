import 'package:flutter/material.dart';

import 'markdown_to_blog.dart';

class BlogView extends StatelessWidget {
  const BlogView(
    this.blogModel, {
    Key? key,
  }) : super(key: key);

  final BlogModel blogModel;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
